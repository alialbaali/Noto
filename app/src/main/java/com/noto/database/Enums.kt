package com.noto.database

enum class NotoColor {
    GRAY,
    BLUE,
    PINK,
    CYAN
}

enum class SortType {
    ASC,
    DESC
}

enum class SortMethod {
    Alphabetically,
    CreationDate,
    ModificationDate,
    Checked,
    Starred,
    Custom
}