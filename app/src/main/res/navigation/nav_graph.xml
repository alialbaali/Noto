<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/notebookListFragment">
    <fragment
        android:id="@+id/notebookListFragment"
        android:name="com.noto.note.ui.NotebookListFragment"
        android:label="NotebookListFragment"
        tools:layout="@layout/fragment_notebook_list">
        <action
            android:id="@+id/action_notebookListFragment_to_notebookFragment"
            app:destination="@id/notebookFragment" />
        <action
            android:id="@+id/action_notebookListFragment_to_todolistListFragment"
            app:destination="@id/todolistListFragment" />
    </fragment>
    <fragment
        android:id="@+id/notebookFragment"
        android:name="com.noto.note.ui.NotebookFragment"
        android:label="NotebookFragment"
        tools:layout="@layout/fragment_notebook">
        <action
            android:id="@+id/action_notebookFragment_to_notebookListFragment"
            app:destination="@id/notebookListFragment" />
        <argument
            android:name="notebook_id"
            android:defaultValue="0L"
            app:argType="long" />
        <argument
            android:name="notebook_title"
            app:argType="string" />
        <argument
            android:name="noto_color"
            android:defaultValue="GRAY"
            app:argType="com.noto.database.NotoColor" />
        <action
            android:id="@+id/action_notebookFragment_to_noteFragment"
            app:destination="@id/noteFragment" />
    </fragment>
    <fragment
        android:id="@+id/noteFragment"
        android:name="com.noto.note.ui.NoteFragment"
        android:label="NoteFragment"
        tools:layout="@layout/fragment_note">
        <action
            android:id="@+id/action_noteFragment_to_notebookFragment"
            app:destination="@id/notebookFragment" />
        <argument
            android:name="note_id"
            android:defaultValue="0L"
            app:argType="long" />
        <argument
            android:name="notebook_id"
            android:defaultValue="0L"
            app:argType="long" />
        <argument
            android:name="notebook_title"
            app:argType="string" />
        <argument
            android:name="noto_color"
            android:defaultValue="GRAY"
            app:argType="com.noto.database.NotoColor" />
    </fragment>
    <fragment
        android:id="@+id/todolistListFragment"
        android:name="com.noto.todo.ui.TodolistListFragment"
        android:label="TodolistListFragment"
        tools:layout="@layout/fragment_todolist_list">
        <action
            android:id="@+id/action_todolistListFragment_to_notebookListFragment"
            app:destination="@id/notebookListFragment" />
        <action
            android:id="@+id/action_todolistListFragment_to_todolistFragment"
            app:destination="@id/todolistFragment" />
    </fragment>
    <fragment
        android:id="@+id/todolistFragment"
        android:name="com.noto.todo.ui.TodolistFragment"
        android:label="TodolistFragment"
        tools:layout="@layout/fragment_todolist">
        <action
            android:id="@+id/action_todolistFragment_to_todolistListFragment"
            app:destination="@id/todolistListFragment" />
        <argument
            android:name="todolist_id"
            app:argType="long" />
        <argument
            android:name="todolist_title"
            app:argType="string" />
        <argument
            android:name="noto_color"
            android:defaultValue="GRAY"
            app:argType="com.noto.database.NotoColor" />
        <action
            android:id="@+id/action_todolistFragment_to_todoFragment"
            app:destination="@id/todoFragment" />
    </fragment>
    <fragment
        android:id="@+id/todoFragment"
        android:name="com.noto.todo.ui.TodoFragment"
        android:label="TodoFragment"
        tools:layout="@layout/fragment_todo">
        <argument
            android:name="todo_id"
            android:defaultValue="0L"
            app:argType="long" />
        <argument
            android:name="todolist_id"
            android:defaultValue="0L"
            app:argType="long" />
        <argument
            android:name="todolist_title"
            app:argType="string" />
        <argument
            android:name="noto_color"
            android:defaultValue="GRAY"
            app:argType="com.noto.database.NotoColor" />
        <action
            android:id="@+id/action_todoFragment_to_todolistFragment"
            app:destination="@id/todolistFragment" />
    </fragment>
</navigation>